[user]
	name = Ben Glassman
	email = ben@vtdesignworks.com
[color]
    ui = always    
[alias]
	s = status
	co = checkout
	fom = fetch origin master
	pod = push origin dev
	pom = push origin master
	fod = fetch origin dev
	df = diff --stat FETCH_HEAD
	dff = diff FETCH_HEAD
	mf = merge FETCH_HEAD
	m = merge -no-ff
	b = branch
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%C(bold blue)<%an>%Creset' --abbrev-commit
[color]
	ui = true
[core]
	excludesfile = /home/benglass/.gitignore
